interface:
  display_name: "Legacy Agentic Workflow Controller"
  short_description: "Resume legacy committed workflow plans."
  default_prompt: "Use $agentic-workflow-controller only to resume a legacy workflow with its committed plan and runtime tracker."
