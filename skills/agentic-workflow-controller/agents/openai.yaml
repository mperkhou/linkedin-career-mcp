interface:
  display_name: "Agentic Workflow Controller"
  short_description: "Track gated workflows and read-only evidence routes."
  default_prompt: "Use $agentic-workflow-controller to start or continue a staged implementation workflow with P steps, G gates, evidence routes, and local JSON state."
