interface:
  display_name: "Legacy Agentic Workflow Init"
  short_description: "Bootstrap legacy committed workflow compatibility."
  default_prompt: "Use $agentic-workflow-init only to bootstrap a legacy committed workflow with its runtime tracker."
